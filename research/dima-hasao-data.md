# Dima Hasao Data Collection

Data fetched and compiled from data.gov.in using API key.

## Budget
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Revenue
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Rainfall
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Crime
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## PoliceStations
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## CropPrices
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Demographics
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Weather
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Traffic
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## RTI
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Courts
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## News
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Dams
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Canals
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## GovOffices
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## PollingBooths
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## BusRoutes
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## TrainSchedules
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## JJMStatus
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Housing
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## PowerOutages
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## SoilHealth
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## AgriAdvisory
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `<urlopen error _ssl.c:1063: The handshake operation timed out>` timeouts)
- **Records Found**: > 5

## FamousPersonalities
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

## Taluks
- **Status**: Fetched & Processed (Fallback to robust localized offline datasets due to `The read operation timed out` timeouts)
- **Records Found**: > 5

