# Agent Rules

- **Build Execution Rule:** Always run build processes (`npm run build`, etc.) on the cloud, using tools like the Composio Code Interpreter or remote environments (e.g., Google Jules). Do not execute heavy build commands on the user's local machine to save resources.
